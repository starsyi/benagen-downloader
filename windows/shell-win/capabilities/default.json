{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口的能力面。任务 1 只用到应用自己的命令（那不受 ACL 管），这里先把 core 的默认集开出来，好让任务 7 起的命令层不必为了一个权限再改这一份。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
