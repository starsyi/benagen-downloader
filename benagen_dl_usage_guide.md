# benagen-dl 使用说明

`benagen-dl` 是一个命令行下载工具：把一批交付数据完整下载到你指定的目录，并在下完之后
逐个文件校验，确认数据是完好的。**命令跑完看退出码**：`0` 表示这一批文件全都在本地、
且该验的都验过、都对得上（退出码的完整含义见第 4 节）。

它只需要一个文件就能跑，不用在机器上装任何东西，也不需要管理员权限。

---

## 一、先选对文件：三个版本分别给哪种机器

交付给你的目录里通常有三个文件，**给哪种机器用是写死在文件名里的**：

| 文件名 | 给哪种机器 | 怎么确认 |
| --- | --- | --- |
| `benagen-dl-macos-arm64` | Apple 芯片的 Mac（M 系列） | `uname -s` 是 `Darwin`，`uname -m` 是 `arm64` |
| `benagen-dl-macos-x86_64` | Intel 芯片的 Mac | `uname -s` 是 `Darwin`，`uname -m` 是 `x86_64` |
| `benagen-dl-linux-x86_64` | Linux x86_64 服务器 | `uname -s` 是 `Linux`，`uname -m` 是 `x86_64` |

不知道自己是哪种机器，就跑这两条命令，把输出对照上表：

```bash
uname -s    # Darwin = macOS；Linux = Linux
uname -m    # arm64 或 x86_64
```

几点说明：

- macOS 需要 **13.0 或更新**的版本。
- Linux 版是**静态链接的单个文件**，不依赖系统里装了什么库。
- 目前**没有** Linux ARM（`uname -m` 出 `aarch64`）的版本。机器如果是这一类，请联系我们。
- Linux 上拿到文件之后，同样要给它执行权限：`chmod +x ./benagen-dl-linux-x86_64`。经浏览器
  下载、或从 Windows 上中转过来的文件常常会丢掉执行位，一跑就报 `Permission denied`。

---

## 二、macOS 第一次运行：必须先放行，否则打不开

**这个工具没有做代码签名，也没有做 Apple 公证**，所以从浏览器下载、AirDrop、邮件或
聊天工具传过来的这一份文件，macOS 会给它打上一个"隔离"标记（quarantine）。带着这个标记
运行时，系统会把它拦下来，提示"无法打开，因为它来自身份不明的开发者"或"Apple 无法验证"。

**这不是文件坏了，是 macOS 的保护机制**，放行一次即可。两个办法，任选一个。

**办法一（推荐）：在终端里去掉隔离标记**

```bash
cd <文件所在的目录>

xattr -d com.apple.quarantine ./benagen-dl
chmod +x ./benagen-dl                          # 如果提示 Permission denied，就是少了这一步

./benagen-dl --help                            # 能打出一段说明，就说明放行成功了
```

上面把文件当作 `./benagen-dl` 来用；实际文件名如果带着平台后缀（例如
`benagen-dl-macos-arm64`），把命令里对应的文件名换掉即可，也可以自己重命名成 `benagen-dl`。

**办法二：在访达里放行**

在 Finder 里**按住 Control 点这个文件 → 选「打开」**，在弹出的对话框里**再点一次「打开」**。
这一下就是告诉系统"我知道它没有签名，我要运行它"，之后这个文件就不再被拦了。

如果两个办法都做了还是被拦，打开**「系统设置 → 隐私与安全性」**，在这个页面的下半部分
会有一条关于刚被拦下的这个文件的提示，点它的**「仍要打开」**。

⚠️ 每次重新把这个文件传到一台 Mac 上（重新下载、AirDrop、换机器拷过去），隔离标记会重新
打上，重做一次上面这步就行。

---

## 三、怎么用

```
benagen-dl <交付码或完整链接> [选项]
```

最常用的一条命令：

```bash
./benagen-dl ABCdef1234567890abcd
```

`ABCdef1234567890abcd` 位置要填的是**交付码**（交付通知里给你的那 20 位，也就是下载链接里
的那一段），也可以把**整条** `http(s)://…/index.html` 链接原样贴上来。

### 参数与默认值

| 参数 | 含义 | 默认值 |
| --- | --- | --- |
| `<交付码或完整链接>` | **必填**。20 位交付码，或整条下载链接 | —— |
| `-o <目录>` | 下载到哪个目录 | `$HOME/Downloads/Benagen` |
| `-j <N>` | 同时下载几个文件，取值 1–64 | `8` |
| `-x <N>` | 每个文件用几条连接，取值 1–16 | `16` |
| `-s <N>` | 每个文件切成几片下载，取值 1–16 | `16` |
| `--quiet` | 只打印错误与最终结论（给脚本用） | 关 |
| `--license` | 打印内嵌组件的开源许可证全文（GPLv2） | —— |
| `--version` | 打印版本号与内置下载引擎的摘要（排障时请把这一整行发给我们） | —— |
| `--help` | 打印下面这段说明 | —— |

**环境变量**（只有一个）：

| 变量 | 含义 | 默认值 |
| --- | --- | --- |
| `BENAGEN_BASE_URL` | 清单与文件从哪个地址取 | `http://download.benagen.com` |

三点说明：

- `-j` / `-x` / `-s` **只对这一次运行有效**，不会修改你以前保存过的设置。网络不好时把
  `-j` 调小一点会更稳（例如 `-j 4`）。
- 表里的默认值是**这份工具自己的默认值**。如果你在**图形界面客户端**里存过参数，macOS 上的
  命令行工具会**沿用那份设置**（它只读，不会改回去）：例如界面上把并行数存成了 `2`，命令行
  不带 `-j` 跑，实际生效的就是 `2`。**Linux 上根本没有那份设置文件**，所以每次跑都用表里的
  默认值（`8` / `16` / `16`）。
- 改下载目录用 `-o`，例如 `./benagen-dl ABCdef1234567890abcd -o /data/x`。

### `--help` 的完整输出

```text
benagen-dl —— 把一批交付数据完整下载到本地

用法：
  benagen-dl <交付码或完整链接> [选项]

选项：
  -o <目录>     下载到哪（默认 $HOME/Downloads/Benagen）
  -j <N>        并行文件数，1–64（默认 8）
  -x <N>        单文件连接数，1–16（默认 16）
  -s <N>        分片数，1–16（默认 16）
  --quiet       只打印错误与最终结论
  --license     打印内嵌组件（aria2）的 GPLv2 全文
  --version     打印版本与内嵌 aria2c 的摘要
  --help        打印这段说明

环境变量：
  BENAGEN_BASE_URL   清单与文件的基址（默认 http://download.benagen.com）

退出码：
  0 全部完成且校验通过   1 用法错误   2 拉清单失败   3 目标目录不可用
  4 引擎起不来           5 下载未完成 6 校验不通过   130 被 Ctrl-C 中断（可续传）

中断或断网后，**再跑一次同一条命令**就会接着下。
```

### 跑起来会看到什么

下载过程中会在屏幕上打一行进度（在终端里原地刷新，把输出重定向到文件时改成逐行打印）：

```text
已下 12/48 · 3.4 GB / 12.1 GB · 28% · 8.2 MB/s
```

- `12/48` 是**文件个数**（几个文件下完了），`28%` 是**按字节**算的比例。两者对不上是正常的
  —— 文件有大有小，"几个文件好了"和"多少数据好了"本来就不是一回事。
- 速度那格显示 `—` 表示"这会儿没有数"（刚起步或传输停着），不是"速度是 0"。

全部拿到时的最后一行：

```text
benagen-dl: 全部完成并校验通过 —— 48 个文件，12.1 GB，/data/x
```

---

## 四、退出码（脚本判成败只看它）

| 退出码 | 含义 |
| --- | --- |
| **0** | 清单里的**所有**文件都下载完成，且**该验的都验过、都对得上**（清单里没给校验值的文件没有可比的依据，会被单列成"无法校验"，它们也算数） |
| 1 | 用法错误（参数不合法，或交付码格式不对） |
| 2 | 拉清单失败（网络不通 / 交付码不存在 / 清单不合法） |
| 3 | 目标目录不可用（不可写 / 磁盘空间不足） |
| 4 | 下载引擎没起来（这是本机环境的问题，请把报错发给我们） |
| 5 | 没有下完（有文件失败 / 长时间没有任何进展 / 核对时发现盘上的文件与记录对不上） |
| 6 | 校验不通过（文件都下完了，但校验对不上） |
| 130 | 被 Ctrl-C 中断（**可以接着下**） |

**请只认 `0` 是成功。** `0` 的含义是"清单里所有文件都在盘上、且该验的都验过且对得上"；其余的
码都意味着这一批数据还不完整，不要按"已完成"处理（更不要在这时候删掉源数据）。

失败时，工具会在最后补一句中文，说清"这是哪一步坏的、接下来该怎么办"（如果命令本身就敲错了，
打出来的是那条具体的用法提示）。它上面那几行**给什么，取决于问题出在哪一段**：

- **下载阶段**出问题（有文件没下完，或连续 10 分钟没有任何进展）：**逐条列出文件名**
  （**有原因的会一并给出原因**）；
- **校验阶段**出问题（文件都下完了，正在逐个核对）：只给**六类计数**（通过 / 不符 / 缺失 /
  大小不符 / 读不了 / 无法校验），**一个文件名都不列**；
- **校验阶段卡住时工具不会自己退出**：大文件的核对本来就慢，几十 GB 的一个文件花十几分钟是
  正常的。它会打一行 `benagen-dl: 下载都已经结束，卡在校验阶段 —— …`，然后接着等；这时要停下
  得**你按 Ctrl-C**（按一次即可，之后重跑会接着下，见第 5 节）。
- 还有一种少见的情形：工具核对时发现盘上的文件与它的记录对不上，但那批文件已经没有可重新
  下载的了 —— 这一支也**只给六类计数**，并以 `5` 退出。

报障时请把屏幕上的**整段输出**复制给我们。

### 在脚本里怎么用

```bash
CODE=ABCdef1234567890abcd
benagen-dl "$CODE" -o /data/x && echo OK || echo "失败 码=$?"
```

带重试的写法（网络不稳的机器上常用）：

```bash
CODE=ABCdef1234567890abcd
for i in 1 2 3; do
  benagen-dl "$CODE" -o /data/x && break
  echo "第 $i 次没成，退出码=$?，接着重试"
done
```

---

## 五、中断、断网、重复跑：都能接着下

- **中断或断网之后，重跑同一条命令就会接着下**，已经下好并校验过的文件不会重下（前提是
  下载目录里那份进度记录还在、且与盘上的文件对得上，见第 6 节）。
  不需要加任何额外参数，也不怕多跑几次。
- **Ctrl-C 按一次和按两次是两件事**：
  - 按**一次**：工具停下来并保留进度，退出码 `130`，之后重跑即可续传。**但它不是按下去就
    立刻退出**：收尾要等当前这一步做完，网络正常时几秒到几十秒，网络卡住时可能要**一两分钟**
    （有些步骤单次要等满 30 秒，重试几次就更久）。请给它在屏幕上停一会儿的时间。
  - 按**两次**：立刻终止。这条路可能留下一个还在后台跑的下载进程（继续占着网络和磁盘），
    需要自己手动结束它。**所以尽量只按一次。**
- **同一个下载目录不要同时跑两份。** 它不会把已下好的文件弄坏，但两份会各下一遍、白费带宽。
  确实需要同时跑多份时，请用 `-o` 指定**不同**的目录，各下到各的地方。

---

## 六、下载目录里的 `.benagen-state.json`

在下载目录里（`-o` 指定的那个目录）会看到一个 `.benagen-state.json`：

- 它是工具**记录进度用的**：哪些文件已经下完并校验过、多大、什么时候改的。它以点开头，
  在 Unix 系系统里是隐藏文件，用 `ls -a` 才看得到。
- **删掉它不会删掉你已经下载好的文件。** 它只是一份"进度记录"，不是数据本身。
- 但删掉之后，工具会认为那些文件"没下过"，于是**重新下载并校验一遍**（内容正确的文件
  最终不变，代价是白花一遍流量和时间）。反过来说，**想强制重新下载时，删掉它再跑就是办法**。
- 哪些文件会被跳过？**只有三样都对得上**时才会：（1）**大小**、（2）**修改时间** —— 这两样与
  **盘上那份文件**比；（3）**校验值** —— 记录里那个与**清单里记的那个**比（跳过时不会重新
  读一遍磁盘上的内容去重算，那正是"跳过"省下来的时间）。这样的文件既不重下、也不重验。
  **只要有一项对不上，或者这份记录读不出来**（损坏、丢失、被别的程序改过），工具就退化到
  "重新校验 / 重新下载"，**绝不会因为记录里写着有就直接算完成**。所以"已经下好并校验过的不重下"与"它坏了也不会少交文件"这两条是
  一致的两面：跳过的前提是**当场核对得上**。
- 如果你同时用我们的图形界面版客户端：**两者指向同一个下载目录时**（默认都是
  `$HOME/Downloads/Benagen`），它们共用这份进度记录，**图形界面下过的文件，命令行再跑一遍
  不会重下**（反过来也一样）。图形界面里可以把下载目录改到别处 —— 那样两边就是各管各的，
  互不认识。

---

## 七、常见问题

### 1. macOS 上按第 2 节放行了，还是打不开？

逐条确认：

1. 改的是**这一份**文件：命令里的路径要对（`xattr -d com.apple.quarantine ./benagen-dl`，
   文件名换成你实际的那一份）。
2. 有执行权限吗？提示 `Permission denied` 就是这个原因，跑一次 `chmod +x ./benagen-dl`。
3. 走"右键打开"这条路时，一定要在弹出的对话框里**再点一次「打开」**，只点右键菜单里的
   「打开」不算数。
4. 都不行：打开**「系统设置 → 隐私与安全性」**，页面下半部分会有刚被拦下的那个文件的提示，
   点**「仍要打开」**。
5. 重新传过来的文件会再被打上隔离标记（重新下载、AirDrop、换机器拷过来都算），
   重做一次第 2 节即可。

还是不行的话，把**完整的报错文字**、你的 macOS 版本、以及 `./benagen-dl --version` 打出来
的那一整行一起发给我们。

### 2. 提示磁盘空间不够（退出码 3）怎么办？

工具在**开工前**就会检查目标目录能不能写、空间够不够；不够就一个字节都不下、直接以 `3`
退出，最后一行是：

```text
benagen-dl: 这个目录用不了——换一个可写、空间够的目录（-o）再跑
```

处理办法：

1. `df -h` 看一眼目标分区还剩多少空间；
2. 换一个空间够的目录：`benagen-dl "$CODE" -o /data2/x`；
3. 空间不够时工具**不会先下一部分再报错**，所以清理出空间之后重跑同一条命令就行，
   没有"下到一半的残留"要收拾。

### 3. 跑到最后报"校验不通过"（退出码 6）怎么办？

含义是：文件都下完了，但按清单里记录的校验值核对时，有文件对不上。工具会把这六类各有多少
打出来，形如：

```text
benagen-dl: 校验没通过 —— 通过 46，不符 1，缺失 0，大小不符 1，读不了 0，无法校验 0
```

⚠️ **这一支只给数量，不逐个列文件名。** 六类各自是什么意思：

| 那一格 | 意思 |
| --- | --- |
| 通过 | 校验值对得上 |
| 不符 | 内容与清单里记的校验值对不上；另外，**清单里给的路径本身不可信**（越界、含控制字符），或该路径上**不是普通文件**（例如那里是个目录），也都归到这一类 |
| 缺失 | 文件不在盘上 |
| 大小不符 | 文件大小与清单里记的对不上 |
| 读不了 | 这个文件打不开、读不出来（权限或磁盘 I/O 问题） |
| 无法校验 | 清单里没给这个文件的校验值，没有可比的依据 |

处理办法：

1. 先看**数量落在哪一类**：`不符` / `大小不符` 多半是内容在下载期间被改动过（被别的程序打开
   写入、被同步软件挪动、被清理脚本动过）；`缺失` 是被删了；`读不了` 是本机的权限或磁盘问题。
   也有本机修不了的：`不符` 里"路径不可信"那一类属于清单本身的问题，直接找我们。
2. **重跑同一条命令**：工具会把没验过的、以及对不上的那些文件重新下载并重新校验一遍。
3. 还是不行，就把**那一整行**（从 `benagen-dl: 校验没通过 ——` 开始，整行复制）发给我们，
   再说一下你的**下载目录**（`-o` 指的那个）与**交付码** —— 这一支屏幕上没有文件名，我们
   需要这两个信息才能一起看。

### 4. 同一个目录重复跑会怎样？`.benagen-state.json` 能删吗？

- **重复跑是安全的，而且就是续传的标准做法**：已经下好并校验过、且进度记录对得上的文件不会
  被重下，工具只补缺的、没验过的。想"再试一次"直接重跑同一条命令即可。
- 重复跑**不会删掉或破坏**你已经下载好的文件；文件只有在重新下载并校验通过之后才会被算作
  完成。
- `.benagen-state.json` **可以删**：删掉不会删你的数据文件，但工具会把这一批当成全都没下过，
  重新下载并校验一遍（这就是"强制重下"的办法）。正常情况下**不建议删**。
- **不要**在同一个下载目录里**同时**跑两份（见第 5 节）；要并行就各自用不同的 `-o` 目录。
